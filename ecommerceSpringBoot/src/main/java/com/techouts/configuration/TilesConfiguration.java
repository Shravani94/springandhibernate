package com.techouts.configuration;

/*import org.springframework.boot.autoconfigure.EnableAutoConfiguration;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.view.UrlBasedViewResolver;
import org.springframework.web.servlet.view.tiles2.TilesConfigurer;
import org.springframework.web.servlet.view.tiles2.TilesView;

@Configuration
@EnableAutoConfiguration*/
public class TilesConfiguration {

	/*
	 * @Bean public UrlBasedViewResolver tilesViewResolver() { UrlBasedViewResolver
	 * tilesViewResolver = new UrlBasedViewResolver();
	 * tilesViewResolver.setViewClass(TilesView.class); return tilesViewResolver; }
	 * 
	 * @Bean public TilesConfigurer tilesConfigurer() { TilesConfigurer
	 * tilesConfigurer=new TilesConfigurer(); String[] defs= {"WEB-INF/tiles.xml"};
	 * tilesConfigurer.setDefinitions(defs); return tilesConfigurer; }
	 * 
	 * 
	 * @Bean public TilesConfigurer tilesConfigurer() { TilesConfigurer
	 * tilesConfigurer = new TilesConfigurer(); String[] defs = {
	 * "WEB-INF/tiles.xml" }; tilesConfigurer.setDefinitions(defs);
	 * tilesConfigurer.setCheckRefresh(true); return tilesConfigurer; }
	 */
}
